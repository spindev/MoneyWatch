services:
  moneywatch:
    image: ghcr.io/spindev/moneywatch:latest
    ports:
      - "8080:3000"
    volumes:
      - moneywatch_data:/data
    restart: unless-stopped

volumes:
  moneywatch_data:
